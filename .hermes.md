# .hermes.md — peixotosite

**Site manifesto** — www.luizpeixoto.com. Hugo + GH Pages + tema casper-two. Destino principal da presenca web do Legal AI Employee.

---

## Repositorio

- **Local:** `$HOME/repos/peixotosite/`
- **Remoto:** `git@github.com:peixoto-ops/peixotosite.git`
- **Branch principal:** `master` (legado)
- **Dominio:** `https://www.luizpeixoto.com`
- **Stack:** Hugo 0.145.0 (extended) + GH Pages + GH Actions

## Estrutura

```
content/
├── post/             → Artigos e posts do blog
├── config.toml       → Configuracao Hugo
├── static/
│   ├── CNAME        → www.luizpeixoto.com
│   └── *.jpg/*.ico  → Assets estaticos
├── themes/
│   └── casper-two/  → Tema (git submodule)
└── .github/workflows/
    └── deploy.yml   → Build + Deploy via GH Pages
```

## Estado Atual

- **Status:** Ativo e no ar
- **Conteudo:** Blog pessoal herdado (posts em portugues sobre ferramentas, privacidade, agenda)
- **Proximo passo (Fase 3):** Reformular como manifesto do Legal AI Employee
- **Visual target:** Fundo escuro aquecido (Warp-like) com toggle para tema claro

## Convencoes

- **NUNCA** commitar direto na master — criar branch de trabalho
- **SEMPRE** testar localmente antes de push (`hugo server -D`)
- Deploy automatico via GH Actions ao push na master
- Submodulo git: `casper-two` tema (puxar com `git submodule update --init --recursive`)
- **Fase 3 prevista:** Adicionar pagina Manifesto, secao Praxis (link vault), secao Servicos

## Rotina de Alinhamento

Toda sessao neste repositorio:
1. `git pull origin master`
2. Verificar ultimo deploy (GH Actions)
3. Testar com `hugo server -D` antes de publicar

## Skills Relacionadas

- `handoff` — Ponto de restauracao de estado
- `writing-plans` — Planejamento de conteudo do site
